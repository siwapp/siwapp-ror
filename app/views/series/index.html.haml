%h1 Series

= form_tag url_for(:controller => "series", :action => "set_default"), :method => :post, :class => "change_default" do

  .table-responsive
    %table.table.table-hover.table-striped.table-bordered
      %thead
        %th{data: {role: "select-all"}} Default
        %th Series Name
      %tbody
        - @series.each do |series|
          %tr
            %td.text-center{data: {role: "select"}}
              = radio_button_tag 'default_series', series.id, checked=series.default
            %td= link_to series.name, edit_series_path(series)
