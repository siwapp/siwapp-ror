<div class="form-group">
  <% if defined?(with_filters) %>
      <button type="button" class="form-control btn btn-default --dropup-- --active--" data-toggle="collapse" data-target="#searchform__filters" aria-pressed="false" autocomplete="off">
        <span class="hidden-xs form-control">Advanced</span>
        <span class="caret"></span>
      </button>
  <% end %>
  <%= f.text_field :with_terms, class: 'form-control', placeholder: 'Search by customer name, email or VAT ID…' %>
  <button type="submit" class="btn btn-primary">
      <span class="glyphicon glyphicon-search"></span>
      &nbsp;Search
  </button>

    <a href="<%= sti_path(current_type()) %>" class="btn btn-default">
      <span class="glyphicon glyphicon-refresh"></span>
    </a>
    
</div><!-- .input-group:terms -->
