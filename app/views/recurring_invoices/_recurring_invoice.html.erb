<tr class="clickable-row" data-href="<%= edit_recurring_invoice_path(recurring_invoice) %>">
  <td><%= link_to recurring_invoice.customer_name, recurring_invoice %></td>
  <td><%= recurring_invoice %></td>
  <td><%= recurring_invoice.period_type %></td>
  <td><%= recurring_invoice.period %></td>
  <td><%= recurring_invoice.get_status %></td>
  <td><%= link_to 'Show', recurring_invoice %></td>
  <td><%= link_to 'Edit', edit_recurring_invoice_path(recurring_invoice) %></td>
  <td><%= link_to 'Destroy', recurring_invoice, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
