%h1
  = @recurring_invoice

= render partial: 'form'

.action-buttons{"data-role": "action-buttons"}
  = link_to t("delete"), @recurring_invoice, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger float-xs-left no-margin'

  = link_to t("back"), :back, class: 'btn btn-secondary'
  %button.btn.btn-success{type: 'button', data: {role: 'submit-form', target: 'form[data-role="invoice"]'}} Save
