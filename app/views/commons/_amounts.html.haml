%table.table
  %tr
    %th{scope: "row"} Subtotal:
    %td= display_money invoice.net_amount
  - invoice.taxes.each do |tax, value|
    %tr
      %th{scope: "row"}= tax
      %td= display_money value
  %tr.amounts-total
    %th{scope: "row"} Total:
    %td= display_money invoice.gross_amount
