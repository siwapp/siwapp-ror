<h1>Editing Invoice</h1>
<p id="alert"><%= alert %></p>
<%= render 'form' %>

<div class="bottom-buttons">
<%= link_to 'Back', invoices_path %> |
<%= link_to 'Destroy', @invoice, method: :delete, data: { confirm: 'Are you sure?' } %> |
<% if @template_url %><%= link_to 'PDF', @template_url + '.pdf' %><% end %>
</div>
