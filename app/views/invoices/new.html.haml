%h1 New Invoice
%p#alert= alert
= render partial: 'form', locals: { is_new: true }
.bottom-buttons
  = link_to 'Cancel', invoices_path, class: 'btn btn-danger'
  = link_to 'Save', '#', class: 'btn btn-success form-save'
