<p id="notice"><%= notice %></p>

<% content_for :new_link do %>
<a class="btn btn-primary" href="<%= new_customer_path %>" role="button">New Customer</a>
<% end %>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Customer Name</th>
      <th>Identification</th>
      <th>Due</th>
      <th>Total</th>
    </tr>
  </thead>

  <tbody class="js-iscroll">
    <% @customers.each do |customer| %>
      <tr class="clickable-row" data-href="<%= edit_customer_path(customer) %>">
        <td><%= customer.name %></td>
        <td><%= customer.identification %></td>
        <td></td>
        <td></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

